h = int(input())  # 시간


count = 0
for i in range(h + 1):
    for j in range(60):
        for k in range(60):
            # 매 시각에 '3'이 포함돼있다면 1씩 증가
            if "3" in str(i) + str(j) + str(k):
                count += 1

num = str(5) + str(13) + str(58)
print("3" in num)
